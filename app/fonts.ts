import { Spectral, Fira_Sans_Condensed } from "next/font/google";
export const spectral = Spectral({
  variable: "--font-spectral-sans",
  subsets: ["latin"], weight: '200'

});

export const fira_sans = Fira_Sans_Condensed({
  variable: "--font-fira-sans-condensed",
  subsets: ["latin"], weight: '200'
});